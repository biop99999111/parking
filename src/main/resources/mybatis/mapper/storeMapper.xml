<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.json.model.StoreMapper">

    <!-- 매장 쿠폰 수량 조회 -->
    <select id="getCouponCount" resultType="int">
        SELECT COUPON_COUNT
        FROM Store
        WHERE store_name = #{storeName}
    </select>

    <!-- 매장 쿠폰 할인 금액 조회 -->
    <select id="getCouponAmount" resultType="int">
        SELECT coupon_amount
        FROM Store 
        WHERE store_name = #{storeName}
    </select>

    <!-- 쿠폰 차감 (1장 감소) -->
    <update id="decreaseCoupon">
        UPDATE Store
        SET COUPON_COUNT = COUPON_COUNT - 1
        WHERE store_name = #{storeName}
          AND COUPON_COUNT > 0
    </update>

    <!-- 차량에 쿠폰 적용 -->
    <update id="applyCouponToCar">
        UPDATE Car
        SET coupon = #{couponAmount},
            storeName = '하이미디어'
        WHERE carNo = #{carNo} 
          AND exitTime IS NULL
    </update>

</mapper>